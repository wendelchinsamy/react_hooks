// Import create context from the react package
import { createContext } from "react"

// Will keep login state and behaviour
// Is a person logged in?
// Log in a person

// We create the context setting the default value to an empty object
export const LoginContext = createContext({})
